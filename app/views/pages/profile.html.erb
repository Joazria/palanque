<div class="profile mt-lg">
  <div class="profile__edit">
    <div class="profile__img">
      <% if @current_user.avatar.attached? %>
    <%= cl_image_tag @current_user.avatar.key,  :width => 40, :height => 40, :gravity => "faces",  class: "profile__img" %>
    <% else %>
    <h2 class="heading-2">FL</h2>
  <% end %>

    </div>
    <div class="profile__info">
      <h2 class="profile__name heading-2">Fulani</h2>
      <p class="profile__location text">Vota em são paulo</p>
      <div class="profile__table mt-md">
        <div class="profile__table-item">
          <small class="text">Favoritos</small>
          <small class="text">32</small>
        </div>
        <div class="profile__table-item">
          <small class="text">Email</small>
          <small class="text"> <%= current_user.email %> </small>
        </div>
      </div>
    </div>
  </div>
  <div class="profile__form">
    <h2 class="profile__form-title title"><%= link_to 'Edit perfil', edit_user_registration_path %></h2>
    <% # render partial: "shared/form-edit-user" %>
  </div>

  <div class="profile__favorite">
    <h2 class="profile__favorite-title title">Favoritos</h2>
    <span class="separator"></span>
    <button class="profile__filter mt-sm">
      <i class="fas fa-filter"></i><p class="text">Filtrar</p>
    </button>
    <div class="profile__favorites-container">
      <%# condicional pra carregar os favoritos %>
      <% @candidates.each do |candidate| %>
        <%= render partial: "shared/card-mini", locals: {
          candidate: candidate
        } %>
      <% end %>
    </div>
  </div>
</div>
