<main class="home">
  <%= @candidate %>
  <div class="search">
    <span class="overlay"></span>
    <div class="search__container">
      <h2 class="heading-2">Conheça seu próximo candidato(a)</h2>
      <%= render "shared/search" %>
      <p class="text"> É muito melhor confiar em alguém quando você conhece essa pessoa, principalmente quando o futuro do seu município, estado ou país depende disso.</p>
    </div>
  </div>
  <div class="candidates">
    <h2 class="heading-2 candidates__title">Quem eleger?</h2>
    <div class="candidates__container mt-lg">
      <% @candidates.shuffle.first(8).each do |candidate| %>
        <%= render partial: "shared/card-horizontal", locals: {
            candidate: candidate,
        } %>
      <% end %>
    </div>
    
  </div>
  <div class="suggestion">
  </div>
</main>
